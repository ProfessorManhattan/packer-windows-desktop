{
  "variables": {
    "boot_command": "",
    "boot_wait": "6m",
    "box_basename": "Windows-Desktop",
    "build_directory": "build",
    "communicator": "winrm",
    "compression_level": "9",
    "cpus": "2",
    "description": "Windows 10 Enterprise (Insider's Preview)",
    "disk_size": "50000",
    "guest_additions_mode": "attach",
    "guest_additions_url": "",
    "headless": "false",
    "hyperv_switch": "bento",
    "iso_checksum": "281281d1d01f9bfd18851b07d8e31254993c0a55c421124fb6d5cb20e4294132",
    "iso_url": "windows.iso",
    "iso_version": "10.0.21354",
    "memory": "4096",
    "restart_timeout": "5m",
    "shutdown_command": "shutdown /s /t 10 /f /d p:4:1 /c \"Packer Shutdown\"",
    "shutdown_timeout": "15m",
    "ssh_password": "vagrant",
    "ssh_username": "vagrant",
    "ssh_wait_timeout": "2h",
    "vagrantup_token": "{{ env `VAGRANT_CLOUD_TOKEN` }}",
    "vagrantup_user": "Megabyte",
    "version_description": "Compact Windows 10 Enterprise (Insider's Preview) 10.0.21354. Includes WinRM and SSH. Bloat removed. See https://gitlab.com/megabyte-labs/packer/windows-desktop for more details.",
    "virtio_file": "virtio-win.iso",
    "vm_name": "{{ user `box_basename` }}-{{ user `iso_version` }}",
    "winrm_password": "vagrant",
    "winrm_timeout": "12h",
    "winrm_username": "vagrant"
  },
  "builders": [
    {
      "type": "hyperv-iso",
      "boot_command": "{{ user `boot_command` }}",
      "boot_wait": "{{ user `boot_wait` }}",
      "communicator": "{{ user `communicator` }}",
      "configuration_version": "8.0",
      "cpus": "{{ user `cpus` }}",
      "disk_size": "{{ user `disk_size` }}",
      "floppy_files": [
        "{{ template_dir }}/Autounattend.xml",
        "{{ template_dir }}/floppy/WindowsPowershell.lnk",
        "{{ template_dir }}/floppy/PinTo10.exe",
        "{{ template_dir }}/scripts/fixnetwork.ps1",
        "{{ template_dir }}/scripts/disable-screensaver.ps1",
        "{{ template_dir }}/scripts/disable-winrm.ps1",
        "{{ template_dir }}/scripts/enable-winrm.ps1",
        "{{ template_dir }}/scripts/microsoft-updates.bat",
        "{{ template_dir }}/scripts/win-updates.ps1",
        "{{ template_dir }}/scripts/openssh.ps1"
      ],
      "guest_additions_mode": "none",
      "headless": "{{ user `headless` }}",
      "iso_checksum": "{{ user `iso_checksum` }}",
      "iso_url": "{{ user `iso_url` }}",
      "memory": "{{ user `memory` }}",
      "output_directory": "{{ user `build_directory` }}/{{ user `box_basename` }}-{{ user `iso_version` }}-Hyper-V",
      "shutdown_command": "{{ user `shutdown_command` }}",
      "shutdown_timeout": "{{ user `shutdown_timeout` }}",
      "ssh_password": "{{ user `ssh_password` }}",
      "ssh_username": "{{ user `ssh_username` }}",
      "ssh_wait_timeout": "{{ user `ssh_wait_timeout` }}",
      "switch_name": "{{ user `hyperv_switch` }}",
      "vm_name": "{{ user `vm_name` }}",
      "winrm_password": "{{ user `winrm_password` }}",
      "winrm_timeout": "{{ user `winrm_timeout` }}",
      "winrm_username": "{{ user `winrm_username` }}"
    },
    {
      "type": "parallels-iso",
      "boot_command": "{{ user `boot_command` }}",
      "boot_wait": "{{ user `boot_wait` }}",
      "communicator": "{{ user `communicator` }}",
      "cpus": "{{ user `cpus` }}",
      "disk_size": "{{ user `disk_size` }}",
      "floppy_files": [
        "{{ template_dir }}/Autounattend.xml",
        "{{ template_dir }}/floppy/WindowsPowershell.lnk",
        "{{ template_dir }}/floppy/PinTo10.exe",
        "{{ template_dir }}/scripts/fixnetwork.ps1",
        "{{ template_dir }}/scripts/disable-screensaver.ps1",
        "{{ template_dir }}/scripts/disable-winrm.ps1",
        "{{ template_dir }}/scripts/enable-winrm.ps1",
        "{{ template_dir }}/scripts/microsoft-updates.bat",
        "{{ template_dir }}/scripts/win-updates.ps1",
        "{{ template_dir }}/scripts/openssh.ps1"
      ],
      "guest_os_type": "win-10",
      "iso_checksum": "{{ user `iso_checksum` }}",
      "iso_url": "{{ user `iso_url` }}",
      "memory": "{{ user `memory` }}",
      "output_directory": "{{ user `build_directory` }}/{{ user `box_basename` }}-{{ user `iso_version` }}-Parallels",
      "parallels_tools_flavor": "win",
      "parallels_tools_mode": "attach",
      "prlctl": [
        ["set", "{{ .Name }}", "--adaptive-hypervisor", "on"],
        ["set", "{{ .Name }}", "--cpus", "{{ user `cpus` }}"],
        ["set", "{{ .Name }}", "--efi-boot", "off"],
        ["set", "{{ .Name }}", "--memsize", "{{ user `memory` }}"],
        ["set", "{{ .Name }}", "--startup-view", "window"],
        ["set", "{{ .Name }}", "--winsystray-in-macmenu", "off"]
      ],
      "prlctl_version_file": ".prlctl_version",
      "shutdown_command": "{{ user `shutdown_command` }}",
      "shutdown_timeout": "{{ user `shutdown_timeout` }}",
      "ssh_password": "{{ user `ssh_password` }}",
      "ssh_username": "{{ user `ssh_username` }}",
      "ssh_wait_timeout": "{{ user `ssh_wait_timeout` }}",
      "vm_name": "{{ user `vm_name` }}",
      "winrm_password": "{{ user `winrm_password` }}",
      "winrm_timeout": "{{ user `winrm_timeout` }}",
      "winrm_username": "{{ user `winrm_username` }}"
    },
    {
      "type": "qemu",
      "accelerator": "kvm",
      "boot_command": "{{ user `boot_command` }}",
      "boot_wait": "{{ user `boot_wait` }}",
      "communicator": "{{ user `communicator` }}",
      "cpus": "{{ user `cpus` }}",
      "disk_size": "{{ user `disk_size` }}",
      "floppy_files": [
        "{{ template_dir }}/Autounattend.xml",
        "{{ template_dir }}/floppy/WindowsPowershell.lnk",
        "{{ template_dir }}/floppy/PinTo10.exe",
        "{{ template_dir }}/scripts/fixnetwork.ps1",
        "{{ template_dir }}/scripts/disable-screensaver.ps1",
        "{{ template_dir }}/scripts/disable-winrm.ps1",
        "{{ template_dir }}/scripts/enable-winrm.ps1",
        "{{ template_dir }}/scripts/microsoft-updates.bat",
        "{{ template_dir }}/scripts/win-updates.ps1",
        "{{ template_dir }}/scripts/openssh.ps1"
      ],
      "headless": "{{ user `headless` }}",
      "iso_checksum": "{{ user `iso_checksum` }}",
      "iso_url": "{{ user `iso_url` }}",
      "memory": "{{ user `memory` }}",
      "output_directory": "{{ user `build_directory` }}/{{ user `box_basename` }}-{{ user `iso_version` }}-KVM",
      "qemuargs": [
        ["-m", "{{ user `memory` }}"],
        ["-smp", "{{ user `cpus` }}"],
        [
          "-drive",
          "file={{ user `build_directory` }}/{{ user `box_basename` }}-{{ user `iso_version` }}-KVM/{{ .Name }},if=virtio,cache=writeback,discard=ignore,format=qcow2,index=1"
        ],
        ["-drive", "file={{ user `iso_url` }},media=cdrom,index=2"],
        ["-drive", "file={{ template_dir }}/{{ user `virtio_file` }},media=cdrom,index=3"]
      ],
      "shutdown_command": "{{ user `shutdown_command` }}",
      "shutdown_timeout": "{{ user `shutdown_timeout` }}",
      "ssh_password": "{{ user `ssh_password` }}",
      "ssh_username": "{{ user `ssh_username` }}",
      "ssh_wait_timeout": "{{ user `ssh_wait_timeout` }}",
      "vm_name": "{{ user `vm_name` }}",
      "winrm_password": "{{ user `winrm_password` }}",
      "winrm_timeout": "{{ user `winrm_timeout` }}",
      "winrm_username": "{{ user `winrm_username` }}"
    },
    {
      "type": "virtualbox-iso",
      "boot_command": "{{ user `boot_command` }}",
      "boot_wait": "{{ user `boot_wait` }}",
      "communicator": "{{ user `communicator` }}",
      "cpus": "{{ user `cpus` }}",
      "disk_size": "{{ user `disk_size` }}",
      "floppy_files": [
        "{{ template_dir }}/Autounattend.xml",
        "{{ template_dir }}/floppy/WindowsPowershell.lnk",
        "{{ template_dir }}/floppy/PinTo10.exe",
        "{{ template_dir }}/scripts/fixnetwork.ps1",
        "{{ template_dir }}/scripts/disable-screensaver.ps1",
        "{{ template_dir }}/scripts/disable-winrm.ps1",
        "{{ template_dir }}/scripts/enable-winrm.ps1",
        "{{ template_dir }}/scripts/microsoft-updates.bat",
        "{{ template_dir }}/scripts/win-updates.ps1",
        "{{ template_dir }}/scripts/openssh.ps1"
      ],
      "guest_additions_mode": "attach",
      "guest_additions_url": "{{ user `guest_additions_url` }}",
      "guest_os_type": "Windows10_64",
      "hard_drive_interface": "sata",
      "headless": "{{ user `headless` }}",
      "iso_checksum": "{{ user `iso_checksum` }}",
      "iso_url": "{{ user `iso_url` }}",
      "memory": "{{ user `memory` }}",
      "output_directory": "{{ user `build_directory` }}/{{ user `box_basename` }}-{{ user `iso_version` }}-VirtualBox",
      "shutdown_command": "{{ user `shutdown_command` }}",
      "shutdown_timeout": "{{ user `shutdown_timeout` }}",
      "ssh_password": "{{ user `ssh_password` }}",
      "ssh_username": "{{ user `ssh_username` }}",
      "ssh_wait_timeout": "{{ user `ssh_wait_timeout` }}",
      "vboxmanage": [
        ["modifyvm", "{{ .Name }}", "--audio", "none"],
        ["modifyvm", "{{ .Name }}", "--cpus", "{{ user `cpus` }}"],
        ["modifyvm", "{{ .Name }}", "--memory", "{{ user `memory` }}"],
        ["modifyvm", "{{ .Name }}", "--vram", "128"],
        ["setextradata", "{{ .Name }}", "VBoxInternal/CPUM/IsaExts/CMPXCHG16B", "1"]
      ],
      "vm_name": "{{ user `vm_name` }}",
      "winrm_password": "{{ user `winrm_password` }}",
      "winrm_timeout": "{{ user `winrm_timeout` }}",
      "winrm_username": "{{ user `winrm_username` }}"
    },
    {
      "type": "vmware-iso",
      "boot_command": "{{ user `boot_command` }}",
      "boot_wait": "{{ user `boot_wait` }}",
      "communicator": "{{ user `communicator` }}",
      "cpus": "{{ user `cpus` }}",
      "disk_adapter_type": "lsisas1068",
      "disk_size": "{{ user `disk_size` }}",
      "disk_type_id": "1",
      "floppy_files": [
        "{{ template_dir }}/Autounattend.xml",
        "{{ template_dir }}/floppy/WindowsPowershell.lnk",
        "{{ template_dir }}/floppy/PinTo10.exe",
        "{{ template_dir }}/scripts/fixnetwork.ps1",
        "{{ template_dir }}/scripts/disable-screensaver.ps1",
        "{{ template_dir }}/scripts/disable-winrm.ps1",
        "{{ template_dir }}/scripts/enable-winrm.ps1",
        "{{ template_dir }}/scripts/microsoft-updates.bat",
        "{{ template_dir }}/scripts/win-updates.ps1",
        "{{ template_dir }}/scripts/openssh.ps1"
      ],
      "guest_os_type": "windows9-64",
      "headless": "{{ user `headless` }}",
      "iso_checksum": "{{ user `iso_checksum` }}",
      "iso_url": "{{ user `iso_url` }}",
      "memory": "{{ user `memory` }}",
      "output_directory": "{{ user `build_directory` }}/{{ user `box_basename` }}-{{ user `iso_version` }}-VMWare",
      "shutdown_command": "{{ user `shutdown_command` }}",
      "shutdown_timeout": "{{ user `shutdown_timeout` }}",
      "ssh_password": "{{ user `ssh_password` }}",
      "ssh_username": "{{ user `ssh_username` }}",
      "ssh_wait_timeout": "{{ user `ssh_wait_timeout` }}",
      "tools_upload_flavor": "windows",
      "version": 14,
      "vm_name": "{{ user `vm_name` }}",
      "vmx_data": {
        "RemoteDisplay.vnc.enabled": "false",
        "RemoteDisplay.vnc.port": 5900
      },
      "vmx_remove_ethernet_interfaces": true,
      "vnc_port_max": 5980,
      "vnc_port_min": 5900,
      "winrm_password": "{{ user `winrm_password` }}",
      "winrm_timeout": "{{ user `winrm_timeout` }}",
      "winrm_username": "{{ user `winrm_username` }}"
    }
  ],
  "post-processors": [
    [
      {
        "type": "vagrant",
        "compression_level": "{{ user `compression_level` }}",
        "output": "{{ user `build_directory` }}/{{ user `box_basename` }}.{{ .Provider }}.box",
        "vagrantfile_template": "{{ template_dir }}/Vagrantfile"
      },
      {
        "type": "vagrant-cloud",
        "access_token": "{{ user `vagrantup_token` }}",
        "box_tag": "{{ user `vagrantup_user` }}/{{ user `box_basename` }}",
        "version_description": "{{ user `version_description` }}",
        "version": "{{ user `iso_version` }}"
      }
    ]
  ],
  "provisioners": [
    {
      "type": "windows-shell",
      "execute_command": "{{ .Vars }} cmd /c \"{{ .Path }}\"",
      "remote_path": "/tmp/script.bat",
      "scripts": ["{{ template_dir }}/scripts/enable-rdp.bat"]
    },
    {
      "type": "powershell",
      "scripts": [
        "{{ template_dir }}/scripts/vm-guest-tools.ps1",
        "{{ template_dir }}/scripts/debloat-windows.ps1",
        "{{ template_dir }}/scripts/vagrant.custom.ps1"
      ]
    },
    {
      "type": "windows-restart",
      "restart_timeout": "{{ user `restart_timeout` }}"
    },
    {
      "type": "powershell",
      "scripts": [
        "{{ template_dir }}/scripts/set-powerplan.ps1",
        "{{ template_dir }}/scripts/disable-windows-defender.ps1"
      ]
    },
    {
      "type": "windows-shell",
      "execute_command": "{{ .Vars }} cmd /c \"{{ .Path }}\"",
      "remote_path": "/tmp/script.bat",
      "scripts": [
        "{{ template_dir }}/scripts/pin-powershell.bat",
        "{{ template_dir }}/scripts/compile-dotnet-assemblies.bat",
        "{{ template_dir }}/scripts/set-winrm-automatic.bat",
        "{{ template_dir }}/scripts/uac-enable.bat",
        "{{ template_dir }}/scripts/dis-updates.bat",
        "{{ template_dir }}/scripts/disable-tasks.bat",
        "{{ template_dir }}/scripts/compact.bat"
      ]
    }
  ]
}
